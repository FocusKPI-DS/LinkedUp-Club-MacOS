// Automatic FlutterFlow imports
import '/backend/backend.dart';
import '/backend/schema/structs/index.dart';
import '/backend/schema/enums/enums.dart';
import '/actions/actions.dart' as action_blocks;
import 'package:ff_theme/flutter_flow/flutter_flow_theme.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'index.dart'; // Imports other custom actions
import '/flutter_flow/custom_functions.dart'; // Imports custom functions
import 'package:flutter/material.dart';
// Begin custom action code
// DO NOT REMOVE OR MODIFY THE CODE ABOVE!

import 'package:audioplayers/audioplayers.dart';

Future stopAllAudio() async {
  // try {
  // Check if the global audio player exists in FFAppState
  //   if (FFAppState().audioPlayer != null) {
  //    await FFAppState().audioPlayer!.stop();
  //   await FFAppState().audioPlayer!.release();
  //   await FFAppState().audioPlayer!.dispose();
  //  FFAppState().audioPlayer = null;
  //   print('Audio stopped and player disposed successfully');
  // } else {
  //   print('No active audio player to stop');
  //  }
  //} catch (e) {
  //  print('Error stopping audio: $e');
}
